import { withRouter } from "react-router";

const Calendar = () => {
    return (
        <div className="App-header">
            Calendar here
        </div>
    );
}

export default withRouter(Calendar);